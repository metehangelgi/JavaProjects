import acm.graphics.*;public class GLabeledRect extends GCompound {	private GRect frame;	private GLabel label;		/* 	 * Constructor: it takes 3 arguments. The width and height of the	 * rectangle and the text that will appear in it. Once you create	 * the GLabel you should center it in rectangle.	 */	public GLabeledRect(double width, double height, String text) {		// Your code starts here				// Your code ends here	}	/* 	 * setFont: this method changes the font of text in GLabel. Once	 * the font is changed you should again center the label in the	 * rectangle.	 */	public void setFont(String font) {		// Your code starts here				// Your code ends here	}	/*	 *  setLabel: this method changes the text in GLabel. Once the	 * text is changed you should again center the label in the	 * rectangle.	 */	public void setLabel(String text) {		// Your code starts here				// Your code ends here	}	/*	 * getLabel: this method just returns the text in the label.	 */	public String getLabel() {		// Your code starts here				// Your code ends here	}	/*	 * recenterLabel: this method centers the label within the rectangle.	 */	private void recenterLabel() {		double x = (frame.getWidth() - label.getWidth()) / 2;		double y = (frame.getHeight() + label.getAscent()) / 2;		label.setLocation(x, y);	}}