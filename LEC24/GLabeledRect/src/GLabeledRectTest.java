/* * (**) In	this problem you will implement	the class * GLabeledRect. GLabeledRect has a label with a string and a box around * it. Comments above the methods explain what every method does in the * class. Your output should look like the following. */import acm.program.*;public class GLabeledRectTest extends GraphicsProgram {	public void run() {		GLabeledRect rect = new GLabeledRect(200, 50, "Hi! My name is...");		rect.setFont("SansSerif-18");		add(rect, 25, 25);		pause(1500);		rect = new GLabeledRect(150, 50, "What?");		rect.setFont("SansSerif-24");		add(rect, 50, 80);		pause(1500);		rect.setLabel("Who?");		pause(1500);		rect.setLabel("Slim Shady");	}}